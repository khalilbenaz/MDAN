# QA Module Configuration
# Generated by MDAN installer
# Keys user_name / communication_language are managed automatically by the installer.
#
# Optional module-specific keys you can add below:
#   test_artifacts: "{project-root}/mdan_output/test-artifacts"   # where QA docs are written
#   risk_threshold_p0: 15   # probability(1-5) x impact(1-5) score at/above which a risk is P0
#   coverage_gate: 80       # minimum % line/branch coverage required by qa-ci-gates

{}
