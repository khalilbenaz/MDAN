description = "Executes the {{name}} tool from the MDAN Method."
prompt = """
Execute the MDAN '{{name}}' tool.

TOOL INSTRUCTIONS:
1. LOAD the tool file from {project-root}/{{mdanFolderName}}/{{path}}
2. READ its entire contents
3. FOLLOW every instruction precisely as specified

TOOL FILE: {project-root}/{{mdanFolderName}}/{{path}}
"""
